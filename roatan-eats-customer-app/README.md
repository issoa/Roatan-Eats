# Roatan Eats Customer App

Connected to Supabase.

## Test

```bash
npm install
npx expo install expo-font react-dom react-native-web @expo/metro-runtime
npx expo start --web --clear
```

Place an order from the cart. It will be saved to Supabase.
